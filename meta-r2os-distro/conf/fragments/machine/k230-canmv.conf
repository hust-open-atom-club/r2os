# SPDX-License-Identifier: MIT
#
# bitbake-setup resolves OE_FRAGMENTS through the layer that also carries
# conf/templates/default, so the machine fragment stays in the distro layer
# for now.  Move it under meta-k230-bsp/conf/fragments/machine/ once the
# fragment lookup across several enabled layers has been verified against a
# real bitbake-setup run.

MACHINE = "k230-canmv"
